# Enable Intel VT-d for VFIO while leaving ordinary host devices in passthrough
# mode unless a driver explicitly attaches them to an IOMMU domain.
GRUB_CMDLINE_LINUX="${GRUB_CMDLINE_LINUX} intel_iommu=on iommu=pt"
